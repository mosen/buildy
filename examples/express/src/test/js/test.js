/**
 * This is the example javascript which will be built at each http request.
 */

var x = "foo",
    y = "bar",
    z = "baz";

function whyNot(character) {
    if (character === 'zoidberg') {
        alert('I dont know!');
    }
}
