// Expresso test suite for builtin tasks

var assert = require('assert'),
    queue = require('../lib/queue'),
    path = require('path');

module.exports = {

    // Builtins Smoke Tests, Just making sure plugins don't explode as a part of a normal queue


//

//

//

//

//
//    'test write' : function(beforeExit, assert) {
//        var q = new queue.Queue('test-write'),
//            r = new Registry(),
//            b = new buildy.Buildy();
//
//        r.load(__dirname + '/../lib/tasks');
//
//        b._registry = r;
//        q.task('files', ['./test/fixtures/test1.js']).task('concat').task('write');
//        q.run(b);
//    }
}